version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
  containers:
    - name: main
      image: sickcodes/docker-osx:big-sur
blocks:
  - name: Production
    dependencies: []
    run:
      when: branch = 'master'
    task:
      # secrets:
      #   - name: rn-proj-pronetplat-aa
      jobs:
        - name: test
          commands:
            - checkout
            - echo "Hello world"
